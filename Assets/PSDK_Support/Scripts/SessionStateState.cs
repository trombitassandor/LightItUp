namespace HyperCasual.PsdkSupport
{
    /// <summary>
    /// Enumerates the different states for the SessionStart location.
    /// </summary>
    public enum SessionStateState
    {
        Loading,
        PendingView,
        Shown,
    }
}
